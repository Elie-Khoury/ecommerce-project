import { AddClassIfRequiredDirective } from './add-class-if-required.directive';

describe('AddClassIfRequiredDirective', () => {
  it('should create an instance', () => {
    const directive = new AddClassIfRequiredDirective();
    expect(directive).toBeTruthy();
  });
});
